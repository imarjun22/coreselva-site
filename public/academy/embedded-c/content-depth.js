/* Deep lesson overrides and completeness references. */
const lesson = id => COURSE.lessons.find(item => item.id === id);

Object.assign(lesson("variables-types"), {
  title: "Variables and data types - complete guide",
  lede: "A variable is a named, typed object with storage, a value, a lifetime and a permitted set of operations. This lesson builds that definition from the first declaration through integer ranges, floating-point limits, fixed-width types, qualifiers and embedded design choices.",
  objectives: [
    "Declare, initialize, assign and print variables without guessing",
    "Name every fundamental arithmetic type and its modifiers",
    "Determine ranges and sizes from the implementation instead of memorizing one machine",
    "Choose ordinary, fixed-width, least-width and fast-width integer types appropriately",
    "Explain overflow, floating-point approximation, qualifiers and initialization rules"
  ],
  sections: [
    { heading: "What exactly is a variable?", html: `<p>Beginners are often told that a variable is a “box.” That picture helps, but the precise C idea is an <b>object</b>: a region of data storage whose contents represent a value. A declaration gives the object a type and usually a name. The type controls how the stored bits are interpreted, which operations are permitted, the required alignment, and at least a minimum range.</p><p>For <code>int temperature = 25;</code>, <code>int</code> is the type, <code>temperature</code> is the identifier, <code>25</code> is an integer literal, and <code>= 25</code> is the initializer. Initialization gives the object its first value as it comes into existence; later assignment replaces its stored value.</p>` },
    { heading: "Declaration, definition, initialization and assignment", html: `<p>A <b>declaration</b> introduces a name and type. A definition provides the object or function itself. Most object declarations are also definitions, but <code>extern int fault_count;</code> declares an object defined elsewhere. Initialization occurs only when an object begins its lifetime. Assignment occurs afterward.</p><div class="callout"><strong>These are not interchangeable</strong><code>int count = 0;</code> defines and initializes. <code>count = 1;</code> assigns. <code>extern int count;</code> declares without defining storage here.</div>` },
    { heading: "How many kinds of variables are there?", html: `<p>There is no single official count called “types of variables,” because people classify them along different axes. By <b>data type</b>, an object may be integer, floating, pointer, array, structure, union, atomic, and so on. By <b>scope</b>, a name may have block, file, function or prototype scope. By <b>storage duration</b>, an object may be automatic, static, thread or allocated. By <b>linkage</b>, a name may have no, internal or external linkage. These classifications answer different questions and can apply simultaneously.</p><p>For example, a file-scope <code>static uint32_t total;</code> is: an unsigned fixed-width integer by type, file-scoped by name visibility, static-duration by lifetime, and internal-linkage by cross-file visibility.</p>` },
    { heading: "The fundamental integer types", html: `<p>C provides <code>char</code>, signed and unsigned integer families, and <code>_Bool</code>. Integer modifiers combine into <code>signed char</code>, <code>unsigned char</code>, <code>short</code>, <code>unsigned short</code>, <code>int</code>, <code>unsigned int</code>, <code>long</code>, <code>unsigned long</code>, <code>long long</code>, and <code>unsigned long long</code>. Plain <code>char</code> is a distinct type whose signedness is implementation-defined.</p><p>The standard guarantees an ordering of minimum capacities, not universal byte counts. A byte is <code>CHAR_BIT</code> bits and is at least 8 bits. <code>sizeof(char)</code> is always 1 by definition; that one C byte is not required to mean eight bits on every conforming target.</p>` },
    { heading: "Floating-point types", html: `<p>The real floating types are <code>float</code>, <code>double</code>, and <code>long double</code>. They represent a finite set of numbers, typically using sign, exponent and significand fields. Most decimal fractions—including 0.1—cannot be represented exactly in binary floating point. Operations round to a nearby representable value.</p><p>Use <code>float</code> when its range and precision meet requirements and the target handles it efficiently; use <code>double</code> when additional precision is required. Do not choose by habit. Inspect <code>&lt;float.h&gt;</code>, quantify acceptable error, and remember that <code>printf</code> variadic arguments promote float to double.</p>` },
    { heading: "void, booleans and character data", html: `<p><code>void</code> represents absence of a value or an incomplete generic pointed-to type; you cannot create an ordinary object of type void. <code>_Bool</code> stores logical 0 or 1; <code>&lt;stdbool.h&gt;</code> supplies the convenient spellings <code>bool</code>, <code>true</code> and <code>false</code>.</p><p>A <code>char</code> stores one C byte and is commonly used for character data, but text encoding determines what its numeric value means. A Unicode character may require multiple UTF-8 bytes, so “one char equals one human character” is not generally true.</p>` },
    { heading: "Sizes and ranges: ask the implementation", html: `<p><code>sizeof(type)</code> reports storage in C bytes. The headers <code>&lt;limits.h&gt;</code>, <code>&lt;stdint.h&gt;</code>, <code>&lt;stdint.h&gt;</code> and <code>&lt;float.h&gt;</code> provide limits such as <code>INT_MIN</code>, <code>INT_MAX</code>, <code>UINT_MAX</code>, <code>SIZE_MAX</code>, <code>FLT_MAX</code> and <code>FLT_EPSILON</code>. Do not hard-code assumptions such as “long is 32 bits”; that differs across common ABIs.</p><p>For an unsigned N-bit type the mathematical range is 0 through 2<sup>N</sup>-1. For the exact-width signed types in modern C implementations, ranges are exposed through macros such as <code>INT32_MIN</code> and <code>INT32_MAX</code>; use the macros rather than recreating them.</p>` },
    { heading: "Fixed-width, least-width and fast-width integers", html: `<p><code>uint32_t</code> exists only when the implementation provides an unsigned integer type exactly 32 bits wide with no padding. It is ideal for 32-bit registers, specified packet fields and file formats. <code>uint_least32_t</code> is the smallest available type with at least 32 bits; <code>uint_fast32_t</code> is an implementation-chosen fast type with at least 32 bits.</p><p>Use <code>size_t</code> for object sizes and array counts returned by <code>sizeof</code>. Use <code>ptrdiff_t</code> for pointer differences. Use <code>uintptr_t</code> only when an implementation supplies an unsigned integer capable of round-tripping a converted object pointer. Domain types should express units where possible.</p>` },
    { heading: "Signedness, overflow and integer promotions", html: `<p>Unsigned arithmetic is modulo one more than its maximum value. This makes wraparound defined, but not automatically desirable. Signed overflow is undefined behavior. Before operations, narrow integer types are usually promoted to int or unsigned int; then the usual arithmetic conversions select a common type.</p><p>A notorious result is comparing negative signed values with unsigned values: the signed operand may convert to a large unsigned number. Turn on warnings, keep related operands in compatible domains, and check ranges before narrowing or multiplying. Casts should document a proven conversion, not silence a warning.</p>` },
    { heading: "Initialization rules by storage duration", html: `<p>Automatic local objects without initializers have indeterminate values; reading such a value can be undefined behavior. Static-duration objects—including file-scope objects and static locals—are zero-initialized before program startup, then any explicit initializer is applied.</p><p>Arrays and structures may use aggregate initializers. Missing aggregate members are initialized as if static, usually zero. Designated initializers such as <code>.baud = 115200U</code> make configuration clearer and resist member reordering mistakes.</p>` },
    { heading: "const, volatile and atomic are qualifiers", html: `<p><code>const</code> prevents modification through the qualified lvalue; it does not necessarily mean compile-time constant and does not make data thread-safe. <code>volatile</code> makes accesses observable to the abstract machine, which is necessary for memory-mapped hardware and some signal/ISR interactions; it does not make compound operations atomic.</p><p><code>_Atomic</code> types and <code>&lt;stdatomic.h&gt;</code> provide language-level atomic operations where supported. Embedded concurrency still requires a hardware/toolchain-aware design. Qualifiers belong to types and can apply at different pointer levels.</p>` },
    { heading: "Choosing a type professionally", html: `<p>Begin from the data's meaning and required range. Use <code>int</code> for ordinary local integer arithmetic when its range is sufficient. Use unsigned types for bit patterns, modular counters and genuinely nonnegative domains—not merely to “get one more positive bit.” Use exact-width types at hardware and wire boundaries. Use <code>size_t</code> for sizes. Use scaled integers or floating point only after quantifying range, resolution, error and execution cost.</p><p>Record units in identifiers: <code>timeout_ms</code>, <code>voltage_mv</code>, <code>adc_raw</code>. Two variables with the same C type may still be semantically incompatible.</p>` }
  ],
  reference: `
    <h3>Declaration grammar you will see most often</h3>
    <table class="reference-table"><thead><tr><th>Form</th><th>Meaning</th><th>Example</th></tr></thead><tbody>
      <tr><td>type name;</td><td>Define an uninitialized automatic object (or zero-initialized static-duration object).</td><td><code>int count;</code></td></tr>
      <tr><td>type name = value;</td><td>Define and initialize an object.</td><td><code>int count = 0;</code></td></tr>
      <tr><td>const type name = value;</td><td>Define an object not modifiable through this name.</td><td><code>const float pi = 3.14159f;</code></td></tr>
      <tr><td>static type name;</td><td>At block scope: persistent storage. At file scope: internal linkage plus static duration.</td><td><code>static uint32_t calls;</code></td></tr>
      <tr><td>extern type name;</td><td>Declare an object whose definition is elsewhere.</td><td><code>extern int system_state;</code></td></tr>
    </tbody></table>
    <h3>Fundamental arithmetic type families</h3>
    <table class="reference-table"><thead><tr><th>Family</th><th>Members</th><th>Typical use</th><th>Portable source of limits</th></tr></thead><tbody>
      <tr><td>Boolean</td><td><code>_Bool</code> / <code>bool</code></td><td>Logical state</td><td><code>stdbool.h</code></td></tr>
      <tr><td>Character</td><td><code>char</code>, <code>signed char</code>, <code>unsigned char</code></td><td>Text code units or raw bytes</td><td><code>CHAR_BIT</code>, <code>CHAR_MIN</code>, <code>UCHAR_MAX</code></td></tr>
      <tr><td>Standard integer</td><td><code>short</code>, <code>int</code>, <code>long</code>, <code>long long</code> plus unsigned forms</td><td>Arithmetic with implementation-sized ranges</td><td><code>limits.h</code></td></tr>
      <tr><td>Exact-width integer</td><td><code>int8_t</code>...<code>uint64_t</code> when available</td><td>Registers and specified binary layouts</td><td><code>stdint.h</code></td></tr>
      <tr><td>Floating</td><td><code>float</code>, <code>double</code>, <code>long double</code></td><td>Approximate real-number calculations</td><td><code>float.h</code></td></tr>
    </tbody></table>
    <h3>Common output specifiers</h3>
    <table class="reference-table"><thead><tr><th>Value type</th><th>printf form</th><th>Important note</th></tr></thead><tbody>
      <tr><td><code>int</code></td><td><code>%d</code> or <code>%i</code></td><td>Signed decimal.</td></tr>
      <tr><td><code>unsigned int</code></td><td><code>%u</code>, <code>%x</code></td><td>Decimal or hexadecimal.</td></tr>
      <tr><td><code>long</code></td><td><code>%ld</code></td><td>The l modifier must match.</td></tr>
      <tr><td><code>size_t</code></td><td><code>%zu</code></td><td>Do not assume it is unsigned long.</td></tr>
      <tr><td><code>double</code></td><td><code>%f</code>, <code>%e</code>, <code>%g</code></td><td>float arguments are promoted to double.</td></tr>
      <tr><td><code>uint32_t</code></td><td><code>%" PRIu32</code></td><td>Use macros from <code>inttypes.h</code>.</td></tr>
    </tbody></table>
    <h3>Naming rules</h3>
    <p>Identifiers contain letters, digits and underscores; they cannot begin with a digit; keywords are unavailable; C is case-sensitive. Names beginning with an underscore are subject to reserved-name rules—application code should avoid inventing underscore-leading global names. Prefer names that reveal purpose and units.</p>`,
  mistakes: [
    { title: "Reading an uninitialized local", explanation: "The object does not contain a dependable default value. Its value is indeterminate and reading it may be undefined behavior.", code: "int count; printf(\"%d\", count);" },
    { title: "Assuming int is always 32 bits", explanation: "C guarantees minimum ranges, not one universal ABI. Query sizeof and limits or use an exact-width type when the boundary demands it." },
    { title: "Using unsigned to hide invalid negatives", explanation: "An invalid -1 converted to unsigned becomes a large value. Model invalidity explicitly with status, optional state or a checked range." },
    { title: "Casting after overflow", explanation: "A wider destination cannot repair a narrow intermediate that already overflowed. Convert an operand before the risky operation.", code: "uint32_t x = (uint32_t)a * b;" },
    { title: "Comparing float with == after calculations", explanation: "Rounding can make mathematically equal paths produce neighboring representable values. Compare using a requirement-derived tolerance when appropriate." },
    { title: "Wrong printf specifier", explanation: "printf is variadic and interprets arguments according to the format. A mismatch is undefined behavior, not a cosmetic issue." },
    { title: "Believing const means compile-time constant", explanation: "A const object is read-only through that lvalue. In C it is not generally an integer constant expression and may still occupy storage." },
    { title: "Believing volatile is thread safety", explanation: "Volatile preserves observable accesses; it does not make read-modify-write indivisible or protect multi-object invariants." }
  ],
  examples: [
    {
      title: "Declare, initialize, assign and print",
      code: `#include <stdio.h>\n\nint main(void)\n{\n    int temperature_c = 25;\n    printf("initial: %d C\\n", temperature_c);\n\n    temperature_c = 27;\n    printf("updated: %d C\\n", temperature_c);\n    return 0;\n}`,
      note: "Initialization gives the first value; assignment later replaces it.",
      walkthrough: [
        { label: "include", text: "stdio.h declares printf so the compiler can check the call." },
        { label: "declaration", text: "int temperature_c = 25 defines one int object and initializes it." },
        { label: "%d", text: "The conversion specification expects an int and prints signed decimal." },
        { label: "assignment", text: "temperature_c = 27 stores a new value in the existing object." },
        { label: "return", text: "Returning zero reports successful hosted-program termination." }
      ]
    },
    {
      title: "Ask the implementation for sizes and limits",
      code: `#include <float.h>\n#include <limits.h>\n#include <stdio.h>\n\nint main(void)\n{\n    printf("CHAR_BIT = %d\\n", CHAR_BIT);\n    printf("sizeof(int) = %zu bytes\\n", sizeof(int));\n    printf("int range = %d to %d\\n", INT_MIN, INT_MAX);\n    printf("float decimal digits = %d\\n", FLT_DIG);\n    return 0;\n}`,
      note: "This prints facts about the compiler/target actually running the code.",
      walkthrough: [
        { label: "limits.h", text: "Defines integer characteristics selected by this implementation." },
        { label: "sizeof", text: "Produces a size_t count in C bytes, hence the %zu specifier." },
        { label: "FLT_DIG", text: "Reports decimal digits that can be rounded into float and back without change." }
      ]
    },
    {
      title: "Fixed-width types and portable printing",
      code: `#include <inttypes.h>\n#include <stdint.h>\n#include <stdio.h>\n\nint main(void)\n{\n    uint32_t timer_ticks = UINT32_C(4000000);\n    int16_t temperature_tenths = INT16_C(-55);\n\n    printf("ticks = %" PRIu32 "\\n", timer_ticks);\n    printf("temperature = %" PRId16 " tenths C\\n", temperature_tenths);\n    return 0;\n}`,
      note: "inttypes.h avoids assuming which standard integer type underlies an exact-width typedef.",
      walkthrough: [
        { label: "UINT32_C", text: "Creates an integer constant suitable for uint32_t where the macro is provided." },
        { label: "PRIu32", text: "Expands to the implementation's correct printf conversion text." },
        { label: "tenths", text: "The name and output state the fixed decimal scale instead of pretending the integer is degrees." }
      ]
    },
    {
      title: "Observe unsigned wrap and avoid signed overflow",
      code: `#include <stdint.h>\n#include <stdio.h>\n\nint main(void)\n{\n    uint8_t sequence = UINT8_MAX;\n    sequence++;\n    printf("wrapped sequence = %u\\n", (unsigned)sequence);\n\n    int32_t a = 50000;\n    int32_t b = 50000;\n    int64_t safe_product = (int64_t)a * b;\n    printf("safe product = %lld\\n", (long long)safe_product);\n    return 0;\n}`,
      note: "Unsigned wrap is defined. Widening one signed operand protects the multiplication intermediate.",
      walkthrough: [
        { label: "UINT8_MAX", text: "The maximum comes from stdint.h instead of a hard-coded 255 assumption." },
        { label: "promotion", text: "The uint8_t value is promoted for printf, and the explicit unsigned cast matches %u." },
        { label: "int64_t", text: "The cast occurs before multiplication, so the product is computed in the wider type." }
      ]
    },
    {
      title: "Aggregate and designated initialization",
      code: `#include <stdbool.h>\n#include <stdint.h>\n#include <stdio.h>\n\ntypedef struct {\n    uint32_t baud;\n    uint8_t data_bits;\n    bool parity_enabled;\n} uart_config_t;\n\nint main(void)\n{\n    uart_config_t config = {\n        .baud = 115200U,\n        .data_bits = 8U,\n        .parity_enabled = false\n    };\n    printf("baud=%u bits=%u parity=%s\\n",\n           (unsigned)config.baud, (unsigned)config.data_bits,\n           config.parity_enabled ? "on" : "off");\n    return 0;\n}`,
      note: "Designated members make a configuration readable and initialize every listed field by name.",
      walkthrough: [
        { label: "typedef", text: "Creates the domain type name uart_config_t for the structure." },
        { label: ".member", text: "A designated initializer selects the structure member explicitly." },
        { label: "?:", text: "The conditional operator selects the displayed text from the boolean state." }
      ]
    }
  ],
  practice: [
    "Create one variable of every standard signed and unsigned integer rank and print sizeof each.",
    "Print INT_MIN, INT_MAX, UINT_MAX, LONG_MIN, LONG_MAX and SIZE_MAX using correct formats.",
    "Demonstrate the difference between initialization and assignment in a five-line program.",
    "Write a checked conversion from int to uint8_t that rejects values outside 0 through UINT8_MAX.",
    "Predict the result of comparing -1 with 1U, then compile with warnings and explain the conversions.",
    "Represent 23.7 degrees as integer tenths and write conversion/printing code.",
    "Compare sizeof(float), sizeof(double), FLT_DIG and DBL_DIG on your compiler.",
    "Show why 0.1 + 0.2 may not compare equal to 0.3 using high-precision output.",
    "Create a structure with designated initializers and intentionally omit one member; observe its value.",
    "Classify three objects by type, scope, storage duration and linkage.",
    "Inspect Cortex-M4 assembly for operations on uint8_t, uint32_t, float and double.",
    "Review an embedded register header and explain why its members are commonly volatile uint32_t."
  ],
  quiz: { q: "Which statement is fully correct?", options: ["Every int is 32 bits", "Initialization gives an object its initial value; assignment later replaces its stored value", "const automatically makes shared data atomic", "Signed overflow always wraps"], answer: 1, why: "The other statements assume guarantees the C language does not provide." }
});

const DEPTH_GUIDES = {
  "course-welcome": {
    terms: [["Learning contract", "For each topic: form a model, predict behavior, run or measure it, explain the evidence, then transfer the idea to a new problem."], ["Hosted C", "A complete operating-system environment supplies program startup, streams, files and process termination."], ["Freestanding C", "The implementation may provide only the core language/library subset; startup and hardware services are platform-defined."], ["Evidence", "Compiler diagnostics, tests, debugger state, waveforms, timing pulses and memory reports answer different questions."], ["Notebook standard", "Record question, prediction, controlled change, observation, explanation and remaining uncertainty."]],
    mistakes: [["Copying before understanding", "A copied program can appear to work while hiding wrong pins, timing, undefined behavior or accidental dependencies."], ["Changing many variables at once", "When several assumptions change together, a success or failure does not identify which model was correct."]]
  },
  "computer-basics": {
    terms: [["CPU", "Fetches, decodes and executes instructions while maintaining registers such as the program counter and stack pointer."], ["Flash/ROM", "Nonvolatile storage commonly holds instructions and constants across power loss."], ["RAM", "Volatile working storage holds stacks, changing global state, buffers and allocated objects."], ["Peripheral", "Hardware outside the CPU core that implements GPIO, timers, converters, communication and other physical services."], ["Bus", "Interconnect that carries addresses, data and control between bus masters and mapped memories/peripherals."], ["Clock", "A timing source advances synchronous logic; different system regions may have different derived clocks."]],
    mistakes: [["Treating bits as self-describing", "A bit pattern gains meaning only from its type, instruction, encoding, register definition or protocol context."], ["Assuming the CPU directly understands C", "The compiler translates C into target instructions; the processor executes those encoded instructions."]]
  },
  "tools-first-program": {
    terms: [["Preprocessing", "Expands includes/macros and conditionally selects tokens before C compilation."], ["Compilation", "Checks the C translation unit and produces target assembly or an internal representation."], ["Assembly", "Encodes target instructions and data into relocatable object files."], ["Linking", "Resolves symbols, chooses final addresses and combines objects/libraries into an image."], ["ELF/executable", "Carries code, data, addresses, symbols and often debug information."], ["Diagnostic", "A compiler/linker report with location and reason; start with the first error and understand every warning."]],
    mistakes: [["Calling every failure a compiler error", "Undefined references occur at link time; crashes and wrong output occur later at run time."], ["Ignoring warnings", "Warnings often identify signedness, truncation, missing declarations and unused state before they become hardware bugs."]]
  },
  "program-anatomy": {
    terms: [["Token", "A keyword, identifier, literal, operator or punctuation unit recognized by the language grammar."], ["Declaration", "Introduces a name and type; some declarations are definitions."], ["Expression", "Computes a value and may have side effects."], ["Statement", "A complete executable/control unit such as an expression statement, return, selection or loop."], ["Block", "A brace-enclosed compound statement that also establishes scope."], ["Function definition", "A return type, name, parameter list and body that provide callable behavior."]],
    mistakes: [["Misplaced semicolon after if/while", "An empty statement becomes the controlled body, so the following block runs unconditionally."], ["Indentation without braces", "Whitespace does not control C execution; braces and grammar do."]]
  },
  "comments": {
    terms: [["Line comment", "Begins with // and ends at the physical line ending."], ["Block comment", "Begins /* and ends */; standard C block comments do not nest."], ["Intent comment", "Explains a reason, invariant, unit, hardware requirement or non-obvious tradeoff."], ["Documentation drift", "A comment becomes harmful when code changes but the asserted behavior does not."], ["Version control", "Stores deleted experiments and history more reliably than permanent commented-out code."]],
    mistakes: [["Narrating syntax", "Comments such as 'increment i' add noise without explaining why the increment exists."], ["Disabling code with nested block comments", "An inner */ can terminate the outer comment early and expose unintended tokens."]]
  },
  "io-formatting": {
    terms: [["String literal", "A null-terminated array of character type created from quoted source text."], ["Escape sequence", "A backslash form such as newline, tab, backslash or escaped quote representing a character in a literal."], ["Conversion specification", "A printf directive whose length modifier and conversion must match the promoted argument type."], ["Field width/precision", "Formatting controls for minimum width, numeric precision or maximum string characters."], ["Stream", "An abstract sequence such as stdout; on bare metal it needs a retargeted transport."], ["Return value", "printf returns characters written or a negative value on error; output can fail."]],
    mistakes: [["Passing a value as the format string", "printf(number) treats bits as an address to format text and is invalid."], ["Mismatched specifier", "Variadic arguments do not carry enough runtime type metadata; a mismatch is undefined behavior."]]
  },
  "type-conversion": {
    terms: [["Integer promotion", "Types narrower than int are generally promoted before arithmetic."], ["Usual arithmetic conversions", "Operands are converted to a common real type/rank before most binary arithmetic."], ["Narrowing", "Conversion to a type with smaller range or precision can change the value."], ["Explicit cast", "Requests a conversion; it documents intent but cannot guarantee the source value is representable."], ["Conversion timing", "Casting an operand changes the operation; casting only the completed result may be too late."], ["Signed-to-unsigned", "The result is congruent modulo one more than the unsigned maximum, often turning negatives into large positives."]],
    mistakes: [["Using casts to silence warnings", "The warning may reveal a real unproven range or signedness mismatch."], ["Casting after integer division", "The fractional information was already discarded before conversion to float."]]
  },
  "constants": {
    terms: [["Literal", "A value written directly in source with a type determined by syntax, base, value and suffix."], ["const-qualified object", "An object not modifiable through that qualified lvalue; not necessarily a compile-time constant."], ["Integer constant expression", "A restricted compile-time-evaluable expression accepted for cases, enum values and some bounds."], ["enum constant", "A named int-valued constant expression."], ["Macro constant", "Token substitution performed before type checking; useful for conditional compilation and some constants."], ["Unit-bearing name", "A name such as timeout_ms that prevents semantically incompatible numeric values from looking interchangeable."]],
    mistakes: [["Magic numbers", "Repeated unexplained literals hide units and make policy changes inconsistent."], ["Assuming const is stored in Flash", "Placement is an implementation/linker decision; const is first a type-system rule."]]
  },
  "operators": {
    terms: [["Arithmetic", "+, -, *, / and % operate after type conversions; integer division truncates toward zero."], ["Comparison", "<, <=, >, >=, == and != yield int 0 or 1."], ["Logical", "&&, || and ! work with truth and short-circuit."], ["Bitwise", "&, |, ^, ~, << and >> manipulate integer representations."], ["Assignment", "Stores a converted right value into a modifiable left object; compound assignments evaluate the left once."], ["Precedence/associativity", "Grammar rules group operators; they do not generally specify operand evaluation order."]],
    mistakes: [["Confusing = and ==", "Assignment changes state and yields the assigned value; equality only compares."], ["Writing side-effect puzzles", "Expressions that depend on unspecified evaluation order are fragile or undefined; split them into sequenced statements."]]
  },
  "booleans": {
    terms: [["False", "Integer/floating zero and null pointer values are false in conditions."], ["True", "Any nonzero scalar value is true, though comparison results are exactly 0 or 1."], ["bool", "The stdbool.h spelling of _Bool, storing normalized logical state."], ["Short-circuit AND", "A && B skips B when A is false."], ["Short-circuit OR", "A || B skips B when A is true."], ["Predicate", "A function/expression whose primary result answers a yes/no question and should be named accordingly."]],
    mistakes: [["Bitwise instead of logical operator", "& evaluates both operands and combines bits; && short-circuits and produces truth."], ["Comparing every boolean to true", "if (ready) is clearer and avoids assumptions about non-bool truth values."]]
  },
  "decisions": {
    terms: [["if", "Executes its statement when the controlling scalar compares unequal to zero."], ["else", "Associates with the nearest unmatched if and handles the false path."], ["else-if chain", "Selects only the first true branch, so range ordering matters."], ["switch", "Selects a case from an integral/enum controlling expression."], ["fallthrough", "Execution continues into the next case without break/return; document only when intentional."], ["default", "Handles values not matched by cases and is vital at external-input boundaries."]],
    mistakes: [["Overlapping ranges in wrong order", "A broad early condition prevents a more severe later condition from ever running."], ["Missing switch break", "Unintended fallthrough performs additional cases and may cause dangerous commands."]]
  },
  "loops": {
    terms: [["for", "Combines initialization, continuation test and iteration expression around a body."], ["while", "Tests before each iteration and may execute zero times."], ["do-while", "Tests after the body and therefore executes at least once."], ["Invariant", "A condition true before/after each iteration that supports reasoning about correctness."], ["Variant", "A quantity that moves toward termination and proves the loop will finish."], ["Bound", "A maximum iteration/work count needed for memory and real-time analysis."]],
    mistakes: [["Off-by-one", "Using <= count accesses one element beyond a zero-based array."], ["Unsigned countdown with i >= 0", "Unsigned values never become negative, so the condition never becomes false."]]
  },
  "break-continue": {
    terms: [["break in loop", "Transfers control after the nearest enclosing loop."], ["break in switch", "Prevents execution from falling into the next case."], ["continue in for", "Jumps to the iteration expression, then retests."], ["continue in while", "Jumps directly to the condition; progress updates in the skipped body may be lost."], ["early exit", "Ends search/validation once the answer is known and can reduce nesting."], ["cleanup path", "Resource-owning functions may need a shared cleanup point rather than many exits."]],
    mistakes: [["Expecting break to leave nested loops", "It exits only the nearest loop or switch."], ["Skipping progress with continue", "A while-loop counter update placed after continue can create an infinite loop."]]
  },
  "arrays": {
    terms: [["Array object", "A fixed number of contiguous elements of one element type."], ["Index", "A zero-based element offset; valid indices are 0 through count-1."], ["Array-to-pointer conversion", "In most expressions an array converts to a pointer to its first element, but the array remains distinct storage."], ["Element count", "sizeof array / sizeof array[0] works only where the operand is an actual array."], ["Multidimensional array", "An array of arrays stored in row-major order."], ["Bound", "C performs no automatic subscript bounds checking; the program must prove every access."]],
    mistakes: [["Passing an array without a count", "The called function usually receives only a pointer and cannot recover element count."], ["Using sizeof on a parameter", "An array parameter is adjusted to pointer type, so sizeof reports pointer size."]]
  },
  "strings": {
    terms: [["C string", "A sequence of char ending at the first zero byte."], ["Length", "Characters before the terminator; strlen excludes the zero byte."], ["Capacity", "Total writable array bytes; maximum string length is normally capacity-1."], ["String literal", "A static array that must not be modified; point to it with const char *."], ["Binary buffer", "May contain zero bytes and therefore needs an explicit length rather than string functions."], ["Encoding", "Maps bytes/code units to text; UTF-8 characters may occupy multiple bytes."]],
    mistakes: [["Comparing strings with ==", "That compares pointer addresses, not character sequences; use strcmp or bounded domain comparison."], ["Losing the terminator", "String functions continue reading beyond the array when no zero byte occurs within valid storage."]]
  },
  "user-input": {
    terms: [["Acquisition", "Read a bounded amount from a stream/device and preserve transport errors."], ["Parsing", "Convert syntax into typed values while detecting missing or extra tokens."], ["Range validation", "Ensure the converted value fits both its C type and product rule."], ["Recovery", "Define what bytes/line state are discarded after malformed or oversized input."], ["strtol", "Reports parse position and range errors more controllably than a casual scanf call."], ["Command boundary", "Interrupt receives bytes; foreground assembles and interprets complete messages."]],
    mistakes: [["Unbounded %s", "It can write more characters than the destination array holds."], ["Ignoring conversion return values", "The destination may remain unchanged or invalid while code acts as though input succeeded."]]
  },
  "pointers-zero": {
    terms: [["Address-of &", "Produces a pointer to an object/function where permitted."], ["Dereference *", "Accesses the pointed-to object; the pointer must be valid, aligned and alive."], ["NULL", "A null pointer value that points to no object; dereferencing it is undefined."], ["Pointer type", "Controls pointed-to operations, alignment assumptions and arithmetic stride."], ["Lifetime", "A pointer does not keep its target alive; dangling pointers retain an address but no valid object."], ["Ownership", "A design rule stating who may free/modify storage and how long borrowers may use it."]],
    mistakes: [["Returning &local", "The automatic object stops existing when the function returns."], ["Uninitialized pointer", "It holds an indeterminate value; dereferencing it can corrupt arbitrary memory."]]
  },
  "pointers-arrays": {
    terms: [["Decay", "Array expressions commonly convert to first-element pointers, except contexts such as sizeof and unary &."], ["Pointer arithmetic", "p+n advances by n elements, not n bytes, within one array object."], ["One-past", "May be formed for comparison/iteration but never dereferenced."], ["Pointer difference", "Subtracting pointers in the same array yields ptrdiff_t elements."], ["Buffer slice", "Pointer plus explicit length/capacity describes a valid subrange."], ["Aliasing", "Multiple lvalues may access the same storage; effective-type/restrict rules constrain optimization-safe access."]],
    mistakes: [["Treating array and pointer as identical", "They behave similarly in expressions but have different size, storage and assignability."], ["Pointer arithmetic across objects", "Only arithmetic within one array and one-past is defined."]]
  },
  "pointer-to-pointer": {
    terms: [["T **", "Pointer to an object whose type is pointer to T."], ["Caller pointer update", "Passing &pointer lets a function replace the caller's pointer value."], ["Two dereferences", "*pp designates the pointer object; **pp designates its T target."], ["Array of pointers", "A contiguous array whose elements independently point elsewhere."], ["argv", "Function parameter commonly represented as char ** or char *[], pointing to argument strings."], ["Qualifier trap", "T ** does not freely convert to const T ** because that could enable writing an incompatible pointer."]],
    mistakes: [["Allocating without clear ownership", "Returning storage through T ** must specify who frees it and on which failures *out changes."], ["Counting stars without drawing objects", "Mechanical reading fails for complex declarations; trace each indirection level and target lifetime."]]
  },
  "functions": {
    terms: [["Prototype", "Declares return and parameter types so calls can be checked before definition/linking."], ["Definition", "Provides the function body and external/internal linkage."], ["Parameter", "A local object initialized from an argument value; C passes everything by value."], ["Return value", "A value converted to the declared return type; returning from void carries none."], ["Output parameter", "Caller-owned storage selected through a validated pointer, often paired with a status return."], ["Contract", "Preconditions, postconditions, side effects, error behavior, units, context and ownership beyond the signature."]],
    mistakes: [["Missing visible prototype", "Old-style implicit assumptions are invalid in modern C and can produce ABI mismatches."], ["Huge multi-purpose function", "Mixed calculation, hardware access and policy become difficult to test and bound."]]
  },
  "scope-storage": {
    terms: [["Block scope", "Name visible from declaration to the end of its enclosing block."], ["File scope", "Name visible from declaration through the translation unit."], ["Automatic duration", "Object lifetime normally spans block entry to exit."], ["Static duration", "Object exists for the entire program and is initialized before startup."], ["Allocated duration", "Storage exists from successful allocation until deallocation."], ["Linkage", "Determines whether declarations in different scopes/translation units denote the same entity."]],
    mistakes: [["Returning pointer to automatic local", "Scope may have ended and lifetime definitely has, leaving a dangling pointer."], ["Using file globals for convenience", "Uncontrolled writers couple modules and make concurrency invariants invisible."]]
  },
  "math-library": {
    terms: [["float variant", "Functions ending f consume/return float; unsuffixed functions generally use double."], ["Domain", "Input set for which a mathematical function has defined real results."], ["Range error", "A valid mathematical result may be too large/small for representation."], ["NaN/infinity", "Special floating values that propagate through many operations and need policy at boundaries."], ["FPU", "Hardware may accelerate core arithmetic but not eliminate library cost for transcendental functions."], ["Fixed point", "Integer representation with an agreed scale, chosen from quantified range/precision/timing needs."]],
    mistakes: [["Assuming decimals are exact", "Binary floating representations approximate most decimal fractions."], ["Using an epsilon without requirements", "Tolerance must scale from measurement/numerical error and magnitude, not an arbitrary magic number."]]
  },
  "callbacks": {
    terms: [["Function pointer", "Stores the address of a compatible function and can be called indirectly."], ["Callback", "Behavior supplied to a generic mechanism for later invocation."], ["Context pointer", "Caller-selected state returned to the callback, avoiding one global instance."], ["Signature compatibility", "Return/parameter types and calling convention must match; casting incompatible functions is unsafe."], ["Invocation context", "Callback may run in foreground, worker or ISR context, each with different legal operations."], ["Lifetime", "Context and callback target must remain valid until deregistration/completion."]],
    mistakes: [["Calling blocking work from ISR callback", "It inflates latency and may invoke non-ISR-safe services."], ["Dangling context", "A stored pointer to an expired local remains callable but invalid."]]
  },
  "recursion-stack": {
    terms: [["Base case", "Stops recursion with a directly solved instance."], ["Progress measure", "Each recursive call must reduce a well-founded quantity toward the base case."], ["Stack frame", "Implementation storage for return state, locals, saved registers and spills."], ["Maximum depth", "Worst valid input/call path, not merely depth observed in one test."], ["Tail call", "A final recursive call that an implementation may optimize; C does not guarantee elimination."], ["Explicit work stack", "A bounded array/queue replacing implicit recursion when resource control matters."]],
    mistakes: [["No reachable base case", "Calls continue until stack corruption/fault."], ["Tiny local arrays in deep recursion", "Per-call storage multiplies by depth and nested interrupt demand."]]
  }
};

function referenceFromTerms(terms) {
  return `<dl>${terms.map(([term, explanation]) => `<dt>${term}</dt><dd>${explanation}</dd>`).join("")}</dl>`;
}

for (const [id, guide] of Object.entries(DEPTH_GUIDES)) {
  const target = lesson(id);
  if (!target) continue;
  if (!target.reference) target.reference = referenceFromTerms(guide.terms);
  if (!target.mistakes) {
    target.mistakes = guide.mistakes.map(([title, explanation]) => ({ title, explanation }));
  }
}

const ADVANCED_DEPTH = {
  "structs-enums-unions": [["Structure", "A record of ordered named members; padding/alignment make its object representation implementation-dependent."], ["Union", "Members overlap the same storage; the program must track which member's value is active."], ["Enumeration", "A distinct integer-compatible type with named constants; validate external integers before treating them as enum values."], ["Tagged union", "An enum discriminator plus union payload makes variant events/messages explicit."], ["Serialization", "Encode each field into a specified byte layout; never assume a raw struct is a portable packet."]],
  "preprocessor-modules": [["Translation unit", "One source file after preprocessing, compiled independently."], ["Header", "A self-contained shared contract of types, macros and declarations, protected against repeated inclusion."], ["Internal linkage", "File-scope static functions/objects remain private to their translation unit."], ["Macro", "Token substitution before type checking; parenthesize parameters/results and avoid duplicated side effects."], ["Module boundary", "Public header plus private implementation and state, with one documented responsibility."]],
  "stdlib-streams": [["Hosted implementation", "Supplies full standard library and operating-system environment."], ["Freestanding implementation", "Guarantees a smaller core; I/O, startup and termination are target-defined."], ["memcpy/memmove", "Copy bytes; memcpy requires non-overlap while memmove supports overlap."], ["Retargeting", "Connects low-level library operations such as write to UART/debug/USB services."], ["Persistence", "Embedded Flash/EEPROM has erase, endurance and power-failure constraints unlike ordinary RAM."]],
  "file-io": [["FILE stream", "Library-managed buffered connection to a file/device; every open and operation can fail."], ["Text/binary mode", "Text may translate line endings; binary preserves bytes subject to object representation."], ["Partial transfer", "fread/fwrite return item counts that can be smaller than requested."], ["Flush/close", "Moves buffered data through library/OS layers but hardware durability requirements may need more."], ["Embedded record", "Version, length, integrity and commit state make nonvolatile settings recoverable."]],
  "dynamic-memory": [["Allocation", "malloc/calloc/realloc request storage and can return NULL."], ["Ownership", "Defines who frees storage and how borrowed aliases are invalidated."], ["Fragmentation", "Free gaps may fail a large request despite sufficient total bytes."], ["Use-after-free", "An address remains in a pointer after the object's lifetime ended."], ["Pool", "Fixed-size preallocated blocks trade flexibility for bounded capacity and timing."]],
  "defensive-c": [["Undefined behavior", "No language requirements after a violated rule such as signed overflow or invalid access."], ["Implementation-defined", "The implementation chooses and documents one behavior, such as plain-char signedness."], ["Unspecified", "One of several allowed behaviors may occur without documentation."], ["Trust boundary", "Validate sizes, ranges, tags and integrity where external data enters."], ["Sanitizer/static analysis", "Host tools expose invalid memory/UB paths but do not replace target timing/hardware tests."]],
  "testing-builds": [["Unit test", "Fast isolated check of pure/module behavior, usually easiest on a host."], ["Integration test", "Checks real boundaries such as driver-peripheral or task-queue interaction."], ["Hardware-in-loop", "Exercises electrical/timing behavior on target equipment."], ["Regression", "A fixed defect leaves a test that would fail if it returns."], ["Reproducible build", "Pinned tools/flags and clean inputs make identical source produce traceable artifacts."]],
  "date-random": [["Wall clock", "Calendar/time-zone representation that may be corrected or jump."], ["Monotonic clock", "Nondecreasing elapsed-time source for durations and deadlines."], ["PRNG", "Deterministic sequence from state/seed, valuable for repeatable tests but not automatically secure."], ["Entropy", "Unpredictability collected and conditioned for security use."], ["Modulo bias", "rand()%n is uneven when generator range is not divisible by n."]],
  "keywords-reference": [["Keyword", "Grammar-reserved word that cannot be an identifier."], ["Library identifier", "Declared by a header and implemented by a library, not a keyword."], ["Constraint", "A standard requirement whose violation needs a diagnostic during translation."], ["API contract", "Validity, ownership, sizes, errors, side effects and context beyond the signature."], ["Implementation manual", "Documents ABI, extensions, limits and target-defined behavior."]],
  "c-projects": [["Milestone", "Small observable vertical slice with one principal new uncertainty."], ["Acceptance test", "Measurable input, environment, expected result and tolerance."], ["Definition of done", "Correctness plus warnings, failure paths, budgets, documentation and explanation."], ["Retrospective", "Record what the result taught and what evidence remains missing."], ["Complexity budget", "Limit simultaneous new hardware, protocol and architecture mechanisms."]],
  "digital-electronics": [["Logic threshold", "Voltage ranges guaranteed to be read as low/high; not one ideal voltage."], ["Drive current", "Output level specifications depend on source/sink current and total port limits."], ["Pull resistor", "Defines released input/open-drain state against leakage/noise."], ["Open drain", "Actively pulls low or releases, enabling safe shared wired logic."], ["Absolute maximum", "Damage boundary, not a recommended operating condition."]],
  "bits": [["Bit position", "Zero-based power-of-two weight selected by 1U<<n."], ["Mask", "Pattern selecting fields for set, clear, toggle or extraction."], ["Read-modify-write", "Read value, change selected bits, write back; may race with concurrent hardware/ISR."], ["Field", "Several adjacent bits encoded with position and mask."], ["Atomic register", "Dedicated set/clear/toggle registers avoid unsafe shared read-modify-write."]],
  "fixed-point": [["Scale", "Agreement mapping stored integer units to a real quantity."], ["Q format", "Binary fractional-bit convention such as Q16.16."], ["Wide intermediate", "Multiplication/add accumulation width needed before rescaling."], ["Rounding", "Deliberate policy instead of silent truncation bias."], ["Saturation", "Clamp to limits rather than modular wrap when required by physical meaning."]],
  "welcome": [["Vector table", "Initial stack pointer and exception-handler addresses at a target-defined boot location."], ["Reset handler", "First code that prepares clocks/runtime memory then calls application entry."], [".data", "Initialized writable objects copied from nonvolatile image into RAM."], [".bss", "Zero-initialized writable objects cleared at startup."], ["Superloop", "Non-returning foreground loop that repeatedly services system responsibilities."]],
  "c-core": [["Fixed-width type", "Exact representation at hardware/protocol boundaries."], ["Side effect", "State or external action beyond producing an expression value."], ["Unit", "Physical/time scale that must remain explicit in names and conversions."], ["Narrow interface", "Small contract minimizing hidden ownership and hardware coupling."], ["Warning discipline", "Treat compiler warnings as design feedback, not cosmetic output."]],
  "architecture": [["Core", "Instruction set, registers, exception behavior and debug architecture."], ["SoC/MCU", "Core plus memory, interconnect, clocks and vendor peripherals."], ["Memory map", "Address ranges routed to Flash, RAM, system and peripheral registers."], ["NVIC", "Cortex-M nested interrupt controller for enable, pending, priority and vectoring."], ["Reference hierarchy", "Datasheet for electrical/package, reference manual for peripherals, core manual for CPU."]],
  "memory": [[".text/.rodata", "Executable instructions and constants normally placed in nonvolatile memory."], [".data/.bss", "Writable static-duration objects occupying RAM with copied or zero initialization."], ["Stack", "Call/exception storage requiring worst-case depth analysis."], ["Heap", "Runtime allocator region with failure/fragmentation policy."], ["Map file", "Linker report connecting symbols/sections to addresses and sizes."]],
  "pointers-volatile": [["Memory-mapped I/O", "Peripheral registers accessed through target-defined addresses and types."], ["volatile", "Requires observable accesses but not atomicity or mutual exclusion."], ["const volatile", "Read-only-through-code object whose value may change externally, common for status registers."], ["Access width", "8/16/32-bit register transactions must match hardware requirements."], ["CMSIS structure", "Typed vendor/core register layouts replace hand-computed offsets while preserving direct access."]],
  "gpio": [["Mode", "Input, output, alternate-function or analog connection."], ["Output type", "Push-pull drives both levels; open-drain drives low/releases."], ["Pull", "Internal weak up/down/none choice for input state."], ["Speed", "Edge-drive/slew configuration affecting signal integrity and power."], ["Debounce", "Time-domain filtering turning mechanical transitions into logical events."]],
  "design": [["BSP", "Board-specific pin/peripheral wiring and electrical polarity."], ["Driver", "Device/peripheral mechanism independent of application policy."], ["Service", "Reusable policy such as event queues, time or rendering."], ["Application", "Product behavior composed from services."], ["Dependency direction", "Higher policy depends on lower mechanisms through narrow interfaces, not cycles."]],
  "fsm": [["State", "Minimal remembered history needed to interpret the next event."], ["Event", "Discrete occurrence presented to the machine."], ["Guard", "Condition required for a transition."], ["Action", "Effect associated with transition or state entry/exit."], ["Transition table", "Explicit mapping of state+event to next state/action, enabling complete testing."]],
  "fifo": [["Head/tail", "Producer and consumer positions in circular storage."], ["Full/empty rule", "Representation must distinguish both, often by leaving one slot or storing count."], ["Overflow policy", "Drop newest/oldest, block, signal or reset—never unspecified."], ["SPSC", "Single-producer/single-consumer ownership can permit simple lock-free coordination with correct atomics/barriers."], ["Watermark", "Occupancy threshold used for flow control and overload observation."]],
  "time": [["Period", "Interval between intended releases."], ["Execution time", "CPU time consumed by one job."], ["Latency", "Delay from event/release to response."], ["Deadline", "Latest acceptable completion."], ["Jitter", "Variation in timing from ideal/nominal."], ["Wrap-safe elapsed", "Unsigned subtraction computes modular elapsed time within the counter range assumptions."]],
  "sync": [["Blind delay", "Waits assumed worst-case time without checking state."], ["Polling", "Repeatedly checks readiness and consumes CPU until done/timeout."], ["Interrupt", "Hardware asynchronously signals an event for bounded-latency service."], ["DMA", "Transfer engine moves data and signals completion."], ["Timeout", "Converts absent/stuck hardware from infinite wait into explicit failure."]],
  "interrupts": [["Source flag", "Peripheral condition that requests service and must be acknowledged correctly."], ["NVIC enable/priority", "Core-side routing and arbitration separate from peripheral enable."], ["Exception frame", "Hardware-saved context enabling interrupted execution to resume."], ["ISR", "Bounded handler that captures/clears/defers rather than running application policy."], ["Critical section", "Shortest exclusion interval protecting a stated invariant."]],
  "scheduler": [["Release", "Time/event when a job becomes eligible."], ["Cooperative", "Current task runs until it returns; longest task bounds dispatch delay."], ["Preemptive", "Scheduler can switch away based on priority/tick/event."], ["Task context", "Function plus state, period/deadline and status."], ["Idle", "No-ready-work state used for sleep/low-power entry."]],
  "rtos": [["Thread", "Independent execution context with its own stack/state."], ["Ready/blocked", "Ready can run; blocked awaits event/time and consumes no CPU."], ["Context switch", "Save current registers/stack state and restore another thread."], ["Mutex/semaphore/queue", "Ownership, counting and message-transfer primitives with distinct semantics."], ["Priority inversion", "High priority waits on low owner while medium work delays release; inheritance/ceiling bound it."]]
};

for (const [id, terms] of Object.entries(ADVANCED_DEPTH)) {
  const target = lesson(id);
  if (!target || target.reference) continue;
  target.reference = referenceFromTerms(terms);
  target.mistakes = target.mistakes || [
    { title: "Memorizing without a model", explanation: `For ${target.title}, be able to derive each operation from the definitions and trace a concrete example.` },
    { title: "Ignoring boundary and failure behavior", explanation: "Test zero, maximum, invalid, timeout and concurrent cases—not only the happy path." }
  ];
}

const SYSTEM_DEPTH = {
  "timers": [["Counter clock", "Timer input after clock-tree/prescaler rules."], ["Prescaler", "Divides source; many timers use PSC+1."], ["Auto-reload", "Defines wrap/update period, commonly ARR+1 counts."], ["Capture", "Latches counter on an input edge for timestamp measurement."], ["Compare", "Triggers action/interrupt/output when counter reaches programmed value."], ["Overflow", "Modular difference handles one-wrap intervals when width/rate assumptions hold."]],
  "pwm": [["Period", "Counter cycle sets PWM repetition frequency."], ["Compare", "Sets active interval/duty according to polarity/mode."], ["Duty cycle", "Active time divided by total period, with endpoint rules checked in manual."], ["Driver stage", "GPIO cannot safely power motors/solenoids; use transistor/bridge, current limit and flyback path."], ["Controller", "Samples feedback at a fixed rate and saturates commanded actuator output."]],
  "uart": [["Frame", "Idle, start, data, optional parity and stop bits."], ["Baud", "Symbol timing agreement; payload throughput includes framing overhead."], ["TX ready/complete", "Data register/FIFO space differs from final stop bit physically sent."], ["RX error", "Overrun, framing, parity and noise flags need explicit policy."], ["FIFO/parser", "ISR captures bytes; foreground assembles bounded messages and validates syntax."]],
  "serial-electrical": [["Logic UART", "MCU pin voltage/timing before a line transceiver."], ["RS-232", "Single-ended bipolar legacy electrical standard requiring conversion."], ["Differential", "Receiver uses wire difference, rejecting common-mode coupled noise."], ["RS-485", "Multi-drop differential bus with direction, biasing and termination design."], ["Packet layer", "Address/type/length/integrity/recovery above raw byte framing."]],
  "keypads-events": [["Matrix", "Rows plus columns reduce pins for many switches."], ["Scan phase", "Drive one row, allow settling, read columns, advance."], ["Debounce", "Stable-time/filter state per key converts bounce into one event."], ["Ghosting", "Multiple keys create false current paths without diodes/policy."], ["Semantic event", "Driver emits KEY_DOWN/UP; application does not know electrical coordinates."]],
  "spi": [["SCK", "Controller-generated serial clock."], ["MOSI/MISO", "Controller-out/peripheral-in and reverse data paths."], ["CPOL/CPHA", "Idle level and sampling/shift edge mode that both endpoints must share."], ["Chip select", "Selects peripheral and often frames command transaction."], ["Full duplex", "Every transmitted bit shifts a received bit, even when one direction is ignored."], ["Bandwidth", "Bits per pixel/command plus clock and software overhead bound display update rate."]],
  "i2c": [["Open-drain", "Devices pull SDA/SCL low or release; pull-ups create high."], ["Start/stop", "Bus conditions framing ownership of a transaction."], ["Address/RW", "Seven/ten-bit target selection plus transfer direction."], ["ACK/NACK", "Receiver response after bytes; NACK is a normal reportable outcome."], ["Clock stretching/arbitration", "Targets may delay; controllers may contend under protocol rules."], ["Stuck-bus recovery", "Timeout and platform-approved clock/reset sequence prevent eternal blocking."]],
  "usb-overview": [["Host", "Schedules transactions, enumerates devices and provides bus policy."], ["Control endpoint 0", "Mandatory request path for descriptors/configuration."], ["Descriptor", "Exact byte structures declaring identity, interfaces and endpoints."], ["Transfer types", "Control, bulk, interrupt and isochronous optimize different guarantees."], ["Class", "CDC/HID/MSC etc define interoperable higher-level behavior."], ["Endpoint buffer", "Ownership and callback context must be explicit like any asynchronous transport."]],
  "dma": [["Request", "Peripheral/event trigger authorizes a transfer beat."], ["Source/destination", "Addresses with fixed/increment and access-width configuration."], ["Count", "Finite transfer length whose units follow configured width."], ["Ownership", "CPU must not mutate a DMA-owned buffer without coherent protocol."], ["Completion/error", "Flags/interrupt transition buffer state and recover failures."], ["Double buffer", "DMA fills one while software owns/processes another."]],
  "analog-circuits": [["RC time constant", "tau=RC describes first-order response speed."], ["Cutoff", "First-order low-pass fc≈1/(2πRC), modified by source/load realities."], ["Op-amp feedback", "Closed-loop circuit sets gain while real range/bandwidth/slew/noise limit behavior."], ["Buffer", "Voltage follower isolates high-impedance source from ADC sampling load."], ["Anti-alias filter", "Reduces above-band energy before sampling ambiguity occurs."]],
  "analog": [["Reference", "ADC/DAC scale and its error directly affect converted units."], ["Resolution", "N bits provide 2^N nominal codes; effective resolution is reduced by noise/errors."], ["Sample time", "Input capacitor must settle through source impedance."], ["Quantization", "Continuous input maps to discrete codes with bounded ideal error."], ["Calibration", "Corrects measured repeatable offset/gain/nonlinearity within a stated model."], ["Saturation", "Out-of-range input clips; software must expose invalidity rather than plausible numbers."]],
  "noise": [["Accuracy", "Closeness to true value."], ["Precision/repeatability", "Closeness of repeated measurements to each other."], ["Resolution", "Smallest representable/detectable change, not guarantee of accuracy."], ["Aliasing", "Out-of-band signals become indistinguishable in samples."], ["Noise budget", "Combine reference, sensor, analog, quantization, layout and timing contributors."], ["Timestamp/validity", "A sample without age and health can mislead downstream decisions."]],
  "power": [["Active current", "Consumption while CPU/peripherals/radio operate."], ["Sleep current", "Mode-specific current with selected retention/wakeup sources."], ["Duty cycle", "Weighted active/sleep durations determine average current."], ["Wake latency", "Time/energy to restart clocks, peripherals and useful work."], ["Clock gating", "Disable unused clocks/peripherals while preserving required state."], ["Measurement", "Instrument the entire board; regulator, LEDs and sensors may dominate datasheet MCU values."]],
  "clock-rtc": [["Oscillator", "Internal/external frequency source with accuracy/startup/power characteristics."], ["PLL/mux/divider", "Clock-tree elements derive system and peripheral domains."], ["Flash latency/voltage", "Maximum safe core frequency depends on supply and memory wait states."], ["RTC", "Low-power calendar/counter domain often using a 32.768 kHz source."], ["Drift ppm", "Fractional frequency error accumulating elapsed-time error."], ["Monotonic vs wall", "Deadlines use non-jumping time; calendar time may synchronize/jump."]],
  "debug": [["Hypothesis", "Falsifiable explanation predicting an observation."], ["Breakpoint/watchpoint", "Stop on execution or data access, with timing disturbance understood."], ["Fault registers", "Core/peripheral status retaining cause/address clues."], ["GPIO pulse", "Low-overhead physical timing marker for scope/logic analyzer."], ["Trace/log", "Bounded event history with timestamp/context and known observer effect."], ["Layer order", "Power → clocks → pin mux → peripheral → interrupt/DMA → driver → application."]],
  "watchdog-faults": [["Watchdog", "Independent countdown reset unless refreshed within allowed progress window."], ["Health vote", "Refresh only after every required subsystem demonstrates progress."], ["Reset cause", "Preserve flags early to distinguish power, pin, software, watchdog and brownout."], ["Fault record", "Minimal integrity-checked PC/status/context safe to write in damaged state."], ["Safe state", "Outputs and retry/backoff behavior preventing reboot loops from becoming hazards."]],
  "production": [["Requirement", "Measurable behavior under stated environment/tolerance."], ["Manufacturability", "Component/process variation, programming and assembly constraints."], ["Testability", "Accessible test points, self-test, IDs and production result records."], ["Tolerance stack", "Worst-case combined component/reference/timing variations."], ["Lifecycle", "Updates, diagnostics, field failure, service and end-of-life data/security."]],
  "verification-ethics": [["Verification", "Evidence implementation meets specified requirements."], ["Validation", "Evidence requirements/system solve real user need."], ["Hazard", "Potential source of harm analyzed through cause, mitigation and residual risk."], ["Traceability", "Link requirement → design → code → test result."], ["Honest state", "Expose invalid/stale/uncertain data instead of rendering confidence."], ["Independent evidence", "Diverse review/test reduces common-assumption failure."]],
  "wireless-iot": [["Layer", "Physical/link/network/application responsibilities diagnosed separately."], ["Loss/duplicate/reorder", "Normal packet behaviors requiring sequence, timeout and idempotency."], ["Energy protocol", "Intervals, retries and listening duty dominate battery use."], ["Authentication", "Proves authorized origin; encryption alone only hides content."], ["Replay", "Old valid command reused; prevent with nonce/counter/freshness."], ["Update path", "Signed, rollback-aware, recoverable firmware lifecycle."]],
  "boot-security": [["Root of trust", "Protected key/code/state anchoring image authentication."], ["Manifest", "Length, target, version, hash and signature metadata."], ["A/B slot", "Preserves bootable image while inactive slot is programmed/verified."], ["Anti-rollback", "Rejects authentic but prohibited vulnerable older version."], ["Handoff", "Validate, quiesce hardware, set vector/stack state and transfer safely."], ["Recovery", "Known path when candidate and/or primary image fails."]],
  "optimization": [["Metric", "Latency, throughput, Flash, RAM, stack or energy—optimize named constraint."], ["Baseline", "Repeatable workload/hardware/toolchain measurement before change."], ["Algorithm", "Reduce total work before tuning individual instructions."], ["Compiler level", "Optimization transforms under valid-C assumptions; UB can surface."], ["Assembly/map", "Evidence of generated work and memory placement, not goal by itself."], ["Regression", "Tests and remeasurement prove speedup preserved behavior."]],
  "tft-ui": [["Transport", "SPI/parallel mechanism moving bytes."], ["Controller driver", "Panel commands, address windows, orientation and pixel format."], ["Graphics", "Clipped pixels/lines/rectangles/fonts independent of app."], ["Widget/state", "Presentation derived from application model and events."], ["Dirty region", "Redraw only changed bounds to reduce bus time."], ["Buffer strategy", "Full frame, line/tile or immediate drawing trade RAM, tearing and complexity."]]
};

for (const [id, terms] of Object.entries(SYSTEM_DEPTH)) {
  const target = lesson(id);
  if (!target || target.reference) continue;
  target.reference = referenceFromTerms(terms);
  target.mistakes = target.mistakes || [
    { title: "Configuring from memory", explanation: "Derive values from the selected device manual, clock tree, schematic and measured signal instead of copying another board's constants." },
    { title: "Testing only the happy path", explanation: "Exercise timeout, disconnection, overflow, invalid input, wraparound and concurrent completion behavior." }
  ];
}
