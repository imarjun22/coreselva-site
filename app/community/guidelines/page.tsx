import type { Metadata } from "next";
import SectionHeading from "@/components/SectionHeading";

export const metadata: Metadata = { title: "Community guidelines" };
const rules = [["Be rigorous, not hostile","Challenge claims with reasoning, measurements and sources. Never turn technical disagreement into a judgment of someone's worth."],["Show enough context","State target, toolchain, schematic assumptions, expected result, actual result and what you already tried."],["Protect people and projects","Do not publish credentials, private source, personal data, unsafe instructions or material you do not have permission to share."],["Mark uncertainty","Separate what you measured from what you inferred. Update the thread when new evidence changes the explanation."],["Make replies teach","A useful answer explains why, names assumptions and offers the next diagnostic step—not merely a code dump."],["Credit the work","Respect licenses and link original authors, documents, repositories and prior answers."]];
export default function GuidelinesPage(){return <div className="page-shell"><section className="section-space"><div className="site-container max-w-5xl"><SectionHeading eyebrow="COMMUNITY STANDARD" title="Clarity with kindness. Evidence without ego." copy="These rules apply to discussions, replies, profiles and project pages."/><div className="mt-12 grid gap-4 sm:grid-cols-2">{rules.map(([title,copy],index)=><article key={title} className="panel p-6"><span className="font-mono text-xs text-yellow">0{index+1}</span><h2 className="mt-4 text-lg font-black">{title}</h2><p className="mt-3 text-sm leading-6 text-white/50">{copy}</p></article>)}</div></div></section></div>}
